import { StyleSheet, Dimensions } from "react-native";

const styles = StyleSheet.create({
  container: {
    marginVertical: 5,
    marginHorizontal: 10,
  },
  imageStyle: {
    height: 300,
    width: "100%",
    borderRadius: 10,
  },
});

export default styles;
