import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    marginRight: 7,
  },
  textStyle: {
    textAlign: "center",
    fontWeight: "normal",
    fontSize: 16,
  },
});

export default styles;
